-- Database initialization script
-- This runs when the database container first starts

-- Create extensions (PostgreSQL only)
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
-- CREATE EXTENSION IF NOT EXISTS "pg_trgm";

-- Example table structure
-- CREATE TABLE IF NOT EXISTS example (
--     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
--     name VARCHAR(255) NOT NULL,
--     description TEXT,
--     created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
--     updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
-- );

-- Create indexes
-- CREATE INDEX IF NOT EXISTS idx_example_name ON example(name);

-- Seed data for development
-- INSERT INTO example (name, description) VALUES
--     ('Example 1', 'First example record'),
--     ('Example 2', 'Second example record');

SELECT 1; -- Placeholder to prevent empty script error
